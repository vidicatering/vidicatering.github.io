// src/app/layout.tsx atau src/app/page.tsx (sesuaikan dengan struktur proyekmu)
import { Metadata } from "next";
import "./globals.css";
import localfont from "next/font/local";
import React, { ReactNode } from "react";
import Nav from "./component/Nav";

const argent = localfont({
  src: [
    {
      path: "../../public/font/argent-thin.otf",
    },
  ],
  variable: "--font-argent",
});

export const metadata: Metadata = {
  title: "Vidi Catering",
  description: "Generated by create next app",
};

// RootLayout definition
export default function RootLayout({ children }: Readonly<{ children: React.ReactNode }>) {
  return (
    <html lang="en" className={`${argent.variable}`}>
      <body>{children}</body>
    </html>
  );
}

// LayoutProps interface
interface LayoutProps {
  children: ReactNode;
}

// Layout component definition
const Layout: React.FC<LayoutProps> = ({ children }) => {
  return (
    <div>
      <Nav />
      <main>{children}</main>
    </div>
  );
};

export { Layout };
